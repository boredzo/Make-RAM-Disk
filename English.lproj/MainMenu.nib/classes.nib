{
    IBClasses = (
        {
            ACTIONS = {endSettingsWindow = id; takeMultiplierFrom = id; }; 
            CLASS = AppDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {multiplierPopup = NSPopUpButton; settingsWindow = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}